meta {
  name: Create Estimate
  type: http
  seq: 30
}

post {
  url: {{host}}/api/estimates
  body: json
  auth: inherit
}

body:json {
  {
    "weightKg": 5.5,
    "serviceLevel": "EXPRESS",
    "insured": true,
    "orderId": "order-id-here"
  }
}
