meta {
  name: Update Delivery Status
  type: http
  seq: 17
}

patch {
  url: {{host}}/api/deliveries/:id/status
  body: json
  auth: inherit
}

body:json {
  {
    "status": "PICKUP_IN_PROGRESS"
  }
}
